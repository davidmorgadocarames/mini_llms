# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/

# Jupyter
.ipynb_checkpoints/

# Model artifacts / training outputs
*.pt
*.pth
*.ckpt
checkpoints/
runs/
wandb/

# Data (downloaded/generated corpora, kept out of git)
data/raw/
data/processed/
*.jsonl.gz
*.bin
mini_llm/data/artifacts/
depth_lab/data/artifacts/
coconut_lab/data/artifacts/
# Fase D: token bins (~2.4GB), the fine-tuning jsonl (~300MB), the prefix-LM
# plan (cuts/order ~9MB) and the batch-hash registry all live here. The
# tokenizer lives here too and is likewise not versioned -- same as Fase A's,
# it is published to the HuggingFace repo and downloaded from there by the demo.
compare_lab/data/artifacts/

# HuggingFace datasets/tokenizers cache
.cache/
.hf_cache/

# OS
.DS_Store
Thumbs.db

# Fase D: rehearsal artifacts from compare_lab/verify (never a real run)
compare_lab/verify/_run/
